{
  "default": [
    {
      "id": "clr-01",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "options": [
        "v-if is for components, v-show is for HTML tags.",
        "v-if conditionally renders/destroys the element in the DOM; v-show only toggles the CSS 'display' property.",
        "v-if is faster for frequent toggling.",
        "v-show is only available in Vue 2."
      ],
      "correct": [
        1
      ]
    },
    {
      "id": "clr-02",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "code": "<ul>\n  <li v-for=\"item in items\" :key=\"item.id\">{{ item.name }}</li>\n  <li v-else>No items found</li>\n</ul>",
      "options": [
        "It will render 'No items found'.",
        "It will render nothing.",
        "It will cause a template compilation error.",
        "It will render an empty <li>."
      ],
      "correct": [
        2
      ]
    },
    {
      "id": "clr-03",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "options": [
        "To tell Vue which CSS styles to apply.",
        "To help Vue's Virtual DOM track which elements have changed, been added, or removed for efficient patching.",
        "To sort the list automatically.",
        "Keys are required by the browser to render lists."
      ],
      "correct": [
        1
      ]
    },
    {
      "id": "clr-04",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "code": "<li v-for=\"item in items\" v-if=\"item.isActive\" :key=\"item.id\">\n  {{ item.name }}\n</li>",
      "options": [
        "v-if",
        "v-for",
        "They have equal priority.",
        "The order in which they are written determines priority."
      ],
      "correct": [
        0
      ]
    },
    {
      "id": "clr-05",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "code": "<________ v-if=\"isLoggedIn\">\n  <h1>Welcome</h1>\n  <button>Logout</button>\n</________>",
      "options": [
        "div",
        "template",
        "block",
        "fragment"
      ],
      "correct": [
        1
      ]
    },
    {
      "id": "clr-06",
      "stageId": "conditional_list_rendering",
      "type": "multiple",
      "options": [
        "v-for=\"n in 10\" (Iterating over a range)",
        "v-for=\"(value, key) in myObject\" (Iterating over object properties)",
        "v-for=\"item in items\" (Iterating over an array)",
        "v-for=\"char in 'Hello'\" (Iterating over a string)"
      ],
      "correct": [
        0,
        1,
        2
      ]
    },
    {
      "id": "clr-07",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "code": "\n<li v-for=\"user in users\" v-if=\"user.isAdmin\">{{ user.name }}</li>",
      "options": [
        "Keep it as is; Vue handles it efficiently.",
        "Use a computed property to pre-filter the 'users' array.",
        "Use a method called inside the template to filter.",
        "Filter the list in the mounted hook and save it to a new ref."
      ],
      "correct": [
        1
      ]
    },
    {
      "id": "clr-08",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "options": [
        "Vue will automatically generate a new key for one of them.",
        "It will cause a runtime warning and likely lead to unpredictable DOM update bugs.",
        "The app will crash immediately.",
        "Only the first item will be rendered."
      ],
      "correct": [
        1
      ]
    },
    {
      "id": "clr-09",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "options": [
        "v-if removes the element from the DOM; v-show toggles CSS display property.",
        "v-show is faster than v-if.",
        "v-if works only with components.",
        "There is no difference."
      ],
      "correct": [
        0
      ]
    },
    {
      "id": "clr-10",
      "stageId": "conditional_list_rendering",
      "type": "single",
      "options": [
        "It causes a syntax error.",
        "v-for has higher priority than v-if in Vue 3, which can lead to unexpected behavior and performance issues.",
        "It makes the code harder to read.",
        "Vue automatically optimizes this pattern."
      ],
      "correct": [
        1
      ]
    }
  ]
}
